class RR {
  private integer d[];
  public float s[4][5];
  private integer o;
};

// ====== main ====== //
main
  local
    integer x;
    integer y;
    integer z;
    float s[4][5];
    RR ss[10];
    RR obj;
    integer count;
  do
    x = 10;
    x = x + 5 + 6 - 3 * 10;
    write(x / 3);
    read(y);
    read(z);
    write(y+z);
    write(y-z);
    write(y*z);
    write(y/z);

    if (y == 10) then
      write(y);
    else
      write(z);
    ;
    count = 0;
    while(count < 10)
      do
        write(count);
        if (count < 7) then
            do
              write(11);
              if (count == 6)
                then
                  do
                    count = count + 1;
                    while(y <> 0)
                      do
                        write(y);
                        y = y - 1;
                      end;
                  end
                else
                  write(1111);
                ;
            end
          else
            write(4444);
          ;
        count = count + 1;
      end;
  end;